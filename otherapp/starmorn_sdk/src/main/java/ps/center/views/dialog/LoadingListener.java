package ps.center.views.dialog;

/* loaded from: classes.jar:ps/center/views/dialog/LoadingListener.class */
public interface LoadingListener {
    void start(CustomDialog customDialog);

    void timeout(CustomDialog customDialog);
}